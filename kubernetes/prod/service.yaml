apiVersion: v1
kind: Service
metadata:
  name: springboot-backend-service-prod
  labels:
    app: springboot-backend
    env: prod
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: springboot-backend
    env: prod
